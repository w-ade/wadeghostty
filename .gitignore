# Refractive preview rig — the lens engine is derived from Aave's shipped source,
# so it stays local. The repo ships no screenshots, so the renders stay local too.
glass.js
preview-glass.html
preview-*.png
